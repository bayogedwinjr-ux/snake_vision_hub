# Enable URL rewriting
RewriteEngine On

# Remove the CORS headers block and OPTIONS handling here.
# We will let the PHP scripts (via cors.php) handle this reliably.

# Standard routing if you have an index.php router (Optional, keep if your API uses a router)
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^(.*)$ index.php [QSA,L]